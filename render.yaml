services:
  - type: web
    name: crm-store
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn crm_project.wsgi
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
